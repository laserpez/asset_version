# prerequisites

- `bzr`
- `go` 1.3
- `mongodb` running on localhost

# install

```bash
$ git clone https://github.com/vise890/asset_version
$ cd asset_version
$ go get
$ go run asset_version.go
```
